rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The browser uses Firebase only for authentication. All club data goes
    // through authorized Next.js server code using the Firebase Admin SDK.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
